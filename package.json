{
  "name": "heritage-archives-patrimoine",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "dev": "pnpm --filter apps/client dev",
    "build": "pnpm --filter apps/client build",
    "test": "pnpm -r test"
  }
}
