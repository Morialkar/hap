{
  "name": "@hap/core",
  "version": "0.0.0",
  "private": true,
  "description": "HAP shared core: schema engine, validation, query model, platform interfaces",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run"
  }
}
